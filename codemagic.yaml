workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      java: 17
    cache:
      cache_paths:
        - ~/.pub-cache
    scripts:
      - name: Gerar pastas Android (Flutter Create)
        script: flutter create --platforms=android .

      - name: Atualizar compileSdk e targetSdk
        script: |
          perl -pi -e 's/compileSdkVersion \d+/compileSdkVersion 34/g' android/app/build.gradle
          perl -pi -e 's/compileSdk \d+/compileSdk 34/g' android/app/build.gradle
          perl -pi -e 's/targetSdkVersion \d+/targetSdkVersion 34/g' android/app/build.gradle
          perl -pi -e 's/targetSdk \d+/targetSdk 34/g' android/app/build.gradle
          echo "Versões do SDK atualizadas para 34."

      - name: Get packages
        script: flutter pub get

      - name: Criar google-services.json
        script: echo "$GOOGLE_SERVICES_JSON" > $CM_BUILD_DIR/android/app/google-services.json

      - name: Aplicar plugin Google Services
        script: |
          perl -pi -e 's/apply plugin: '\''com.android.application'\''/apply plugin: '\''com.android.application'\''\napply plugin: '\''com.google.gms.google-services'\''/g' android/app/build.gradle
          echo "Plugin do Firebase injetado com sucesso."

      - name: Clean build
        script: flutter clean

      - name: Build APK
        script: flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/*.apk

    publishing:
      email:
        recipients:
          - leoniltonfernandesguaripuna@gmail.com
        notify:
          success: true
          failure: true
